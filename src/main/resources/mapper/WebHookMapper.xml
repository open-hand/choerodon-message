<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="io.choerodon.notify.infra.mapper.WebHookMapper">
    <select id="selectBySendSetting" resultType="io.choerodon.notify.infra.dto.WebHookDTO">
        SELECT
        wh.*
        FROM
        notify_webhook wh
        LEFT JOIN NOTIFY_WEBHOOK_MESSAGE_SETTING wms ON wh.ID = wms.WEBHOOK_ID
        WHERE
        wms.SEND_SETTING_ID = #{send_setting_id}
        AND wh.PROJECT_ID =#{project_id}
    </select>

    <select id="selectWebHookAll" resultType="io.choerodon.notify.infra.dto.WebHookDTO">
        SELECT
        nw.id,
        nw.name,
        nw.type,
        nw.webhook_path,
        nw.project_id,
        nw.enable_flag,
        nw.object_version_number
        FROM
        notify_webhook nw
        LEFT JOIN notify_webhook_message_setting nwms
        ON nw.ID=nwms.WEBHOOK_ID
        LEFT JOIN notify_send_setting nss
        ON nss.ID=nwms.SEND_SETTING_ID
        LEFT JOIN notify_send_setting_category nsc
        ON nsc.`CODE`=nss.CATEGORY_CODE
        where project_id=#{projectId}
        <if test="name !=null">
            AND nw.name=#{name}
        </if>
        <if test="type !=null">
            AND nw.type=#{type}
        </if>
        <if test="enableFlag !=null">
            AND nw.enable_flag=#{enableFlag}
        </if>
        <if test="params !=null">
            AND (
            nw.name LIKE concat(concat('%',#{params}),'%') OR
            nw.type LIKE concat(concat('%',#{params}),'%')
            )
        </if>
    </select>
</mapper>
