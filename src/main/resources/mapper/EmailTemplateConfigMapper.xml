<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="io.choerodon.message.infra.mapper.EmailTemplateConfigMapper">

    <select id="selectByTenantId" resultType="io.choerodon.message.infra.dto.EmailTemplateConfigDTO">
        select * from email_template_config where tenant_id = #{tenantId}
    </select>

</mapper>